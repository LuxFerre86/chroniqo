spring:
  application:
    name: chroniqo
  datasource:
    url: jdbc:mariadb://192.168.100.119:3306/chroniqo?useSSL=false
    username: chroniqo
    password: chroniqo
    #driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    #show-sql: true
    open-in-view: false
    properties:
      hibernate:
        ddl-auto: validate
        #format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.tool.schema: debug